python-dotenv
azure-identity
azure-ai-projects==1.0.0b10

# python -c "import agent_framework.azure as az; print([n for n in dir(az) if not n.startswith('_')])"
# If AzureAIAgentClient is not listed, either install the required package or check your installation.
# .venv\Scripts\pip install -U agent_framework